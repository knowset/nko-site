import { FC } from "react";

// FixMe: make exit button
export const SignOutButton: FC<{}> = () => {
    return (<div>
        {/* <button onClick={() => signOut()}>Sign out</button> */}
    </div>)
}